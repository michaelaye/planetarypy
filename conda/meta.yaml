{% set version = "0.67.1" %}

package:
  name: planetarypy
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/p/planetarypy/planetarypy-{{ version }}.tar.gz
  sha256: 82dff68a9f974a411dcde88439b7922c67af6147ab13fc05b644a9f6fd5f1848

build:
  entry_points:
    - plp = planetarypy.cli:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.11
    - hatchling
    - pip
  run:
    - python >=3.11
    - astropy
    - typer >=0.9
    - fastcore
    - numpy
    - pandas
    - pooch
    - pvl
    - lxml
    - python-dateutil
    - requests
    - tomlkit
    - tqdm
    - yarl
    - loguru
    - python-duckdb
    - rasterio
    - pyproj
    - rioxarray

test:
  imports:
    - planetarypy
    - planetarypy.constants
    - planetarypy.pds
  commands:
    - pip check
    - plp --help
  requires:
    - pip
    - python

about:
  home: https://github.com/planetarypy/planetarypy
  summary: Python tools for planetary science data access — PDS indexes, SPICE kernels, instrument data, and per-body constants
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - michaelaye
